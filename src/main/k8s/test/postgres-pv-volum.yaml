kind: PersistentVolume
apiVersion: v1
metadata:
  name: postgres-pv-volume-env-test
  labels:
    type: local
    app: postgres
spec:
  storageClassName: manual
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/mnt/env-test/data"
