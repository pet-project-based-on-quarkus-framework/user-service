kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: postgres-pv-claim-env-dev
  labels:
    app: postgres
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
